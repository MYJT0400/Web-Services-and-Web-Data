# Book Insights API

Book Insights API is a **Django + Django REST Framework + SQLite** project for managing and exploring a Goodreads books dataset. It provides RESTful CRUD endpoints, searchable book records, a browser-based control page, Swagger documentation, API-key protected write operations, and a hybrid similar-books recommendation system.

## Project Scope

- Dataset: Goodreads `books.csv` from Kaggle.
- Core model: `Book`, aligned with the Goodreads CSV fields.
- Database: SQLite (`books.db`) using Django ORM.
- API style: RESTful JSON API with Django REST Framework.
- UI: browser page at `/` for search, record actions, health check, and similar-books exploration.
- Documentation: Swagger UI at `/docs`, plus Markdown/PDF API documentation under `docs/`.
- Authentication: `X-API-Key` required for create, update, and delete.
- Recommendations: title embedding similarity plus metadata reranking and diversity penalties.

## Quick Start

1. Create and activate a virtual environment.

2. Install dependencies:

   ```bash
   pip install -r requirements.txt
   ```

3. Initialize the database and embeddings:

   ```bash
   python manage.py initbooks
   ```

4. Run the Django development server:

   ```bash
   python manage.py runserver
   ```

5. Open the browser UI:

   ```text
   http://127.0.0.1:8000/
   ```

6. Open Swagger UI:

   ```text
   http://127.0.0.1:8000/docs
   ```

If `.models/fastembed-bge-small-en-v1.5` is missing, `python manage.py initbooks` prints a terminal message and downloads the embedding model automatically. The `.models/` directory is ignored by git so the model does not need to be submitted unless the deployment environment cannot download it.

## API Key

Protected endpoints require this header:

```text
X-API-Key: coursework-demo-key
```

To set your own key in PowerShell:

```powershell
$env:API_KEY="your-secret-key"
```

Swagger UI also supports this API key. Open `/docs`, click `Authorize`, enter `coursework-demo-key` in `ApiKeyAuth`, and then use `POST`, `PUT`, or `DELETE` from the Swagger page. Read-only endpoints remain public.

## Implemented Endpoints

- `GET /` - browser control page.
- `GET /health` - service health check.
- `GET /books` - list books or search with query filters.
- `GET /books/{book_id}` - get one book by internal database ID.
- `GET /books/{book_id}/recommendations` - get similar books with score breakdown and generated reason text.
- `POST /books` - create a book, protected by API key.
- `PUT /books/{book_id}` - update a book, protected by API key.
- `DELETE /books/{book_id}` - delete a book, protected by API key.
- `GET /docs` - Swagger UI generated by drf-spectacular.
- `GET /schema/` - OpenAPI schema.

## Book Fields

The API stores and returns these Goodreads-aligned fields:

- `id`: internal database ID.
- `bookID`: original Goodreads book ID, or auto-assigned for new records.
- `title`
- `authors`
- `average_rating`
- `isbn`
- `isbn13`
- `language_code`
- `num_pages`
- `ratings_count`
- `text_reviews_count`
- `publication_date`
- `publisher`

Create example:

```json
{
  "bookID": 100001,
  "title": "Clean Code",
  "authors": "Robert C. Martin",
  "average_rating": 4.4,
  "isbn": "0132350882",
  "isbn13": "9780132350884",
  "language_code": "eng",
  "num_pages": 464,
  "ratings_count": 1000,
  "text_reviews_count": 100,
  "publication_date": "8/1/2008",
  "publisher": "Prentice Hall"
}
```

## Search

`GET /books` supports these optional filters:

- `skip`
- `limit`
- `book_id`
- `title`
- `authors`
- `isbn`
- `isbn13`
- `language_code`
- `publisher`

Example:

```text
http://127.0.0.1:8000/books?title=Harry%20Potter&authors=Rowling&limit=5
```

`book_id` matches the Goodreads `bookID` field only. Path parameters such as `/books/1` use the internal database `id`.

## Similar-Books Recommendation

`GET /books/{book_id}/recommendations?limit=5` returns similar books with:

- `recommendation_score`
- `score_breakdown`
- `reason`

The recommendation algorithm uses:

- Title embedding similarity: 50%
- Authors match: 15%
- Language code match: 10%
- Publisher match: 5%
- Average rating: 10%
- Ratings count: 10%

Penalty fields in `score_breakdown`:

- `duplicate_penalty`: penalty when the candidate has the same normalized title as the selected target book.
- `title_diversity_penalty`: penalty when the candidate repeats a normalized title that has already appeared earlier in the recommendation list.
- `authors_diversity_penalty`: progressive penalty for repeating authors that already appeared earlier in the recommendation list.
- `language_diversity_penalty`: progressive penalty for repeating the same language code.
- `publisher_diversity_penalty`: progressive penalty for repeating the same publisher.
- `diversity_penalty`: total list-level diversity penalty.

Embedding details:

- Embedding library: `fastembed`
- Model name: `BAAI/bge-small-en-v1.5`
- Local model cache path: `.models/fastembed-bge-small-en-v1.5`
- Embeddings are computed from book titles using FastEmbed on ONNX Runtime, without PyTorch.
- If a complete FastEmbed snapshot is already present locally, the app registers a local FastEmbed model and loads it without contacting Hugging Face.
- Title embeddings are stored in `books.db` after `python manage.py initbooks`.

## Data Import and Startup

- `books.csv` should be placed in the project root.
- `books.db` is stored in the project root.
- The `books` table is created automatically by `python manage.py initbooks`.
- If the database is empty, `books.csv` is imported by `python manage.py initbooks`.
- Missing embedding columns are added automatically for existing local databases.
- Missing title embeddings are precomputed and stored by `python manage.py initbooks`.

## API Documentation

- Markdown: `docs/API_Documentation.md`
- PDF: `docs/API_Documentation.pdf`
- Swagger UI: `http://127.0.0.1:8000/docs`

If you update the API, regenerate or update the PDF documentation so it matches `docs/API_Documentation.md`.

## Project Structure

```text
app/
  auth.py              API key permission class
  crud.py              Django ORM CRUD and search operations
  database.py          project paths for books.db and model cache
  models.py            Django Book model mapped to the books table
  recommendations.py   embedding model loading, scoring, reranking, reasons
  serializers.py       Django REST Framework serializers
  seed.py              database initialization, CSV import, embedding warmup
  ui.py                browser home page HTML/CSS/JavaScript
  urls.py              app URL routes
  views.py             DRF API views and browser page
bookinsights/
  settings.py          Django settings
  urls.py              project URL routes, Swagger schema/docs
  wsgi.py              Django WSGI entrypoint
  asgi.py              Django ASGI entrypoint
docs/
  API_Documentation.md
  API_Documentation.pdf
books.csv
books.db
manage.py
requirements.txt
wsgi.py
```

## Notes for Coursework

- Django REST Framework is used because it provides RESTful JSON endpoints, strong serializer-based validation, and Swagger/OpenAPI documentation.
- SQLite is used because it satisfies the SQL database requirement while keeping setup simple for local execution and marking.
- GenAI-assisted development should be declared in the technical report, including its use for planning, debugging, dataset import, documentation, and recommendation-system design.
